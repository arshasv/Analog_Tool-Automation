# rendering-script-defer-async

**Category:** Rendering Performance

Load scripts with `defer`/`async` when execution order allows.

- Prefer non-blocking script loading in browser apps.
- Avoid parser-blocking scripts in initial document path.
