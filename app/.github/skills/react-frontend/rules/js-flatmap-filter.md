# js-flatmap-filter

**Category:** JavaScript Performance

Use `flatMap` when mapping and filtering can be merged.

- Prefer single-pass transform where readable.
- Avoid multi-pass pipelines creating extra arrays.
