# rendering-animate-svg-wrapper

**Category:** Rendering Performance

Animate wrapper elements instead of complex SVG internals when possible.

- Prefer transforms on container elements.
- Avoid costly SVG re-layout/repaint paths.
