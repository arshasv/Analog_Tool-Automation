# js-index-maps

**Category:** JavaScript Performance

Build lookup maps for repeated key-based access.

- Prefer `Map` for repeated lookups in large collections.
- Avoid repeated linear scans for same keys.
