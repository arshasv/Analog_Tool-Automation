# advanced-use-latest

**Category:** Advanced Patterns

Use a `useLatest` pattern to access current values from stable callbacks.

- Prefer latest-value refs for async/event boundaries.
- Avoid stale closure reads in delayed code paths.
