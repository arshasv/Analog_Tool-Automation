# rendering-hoist-jsx

**Category:** Rendering Performance

Hoist static JSX out of frequently rerendered components.

- Prefer top-level constants for static subtrees.
- Avoid recreating unchanged JSX every render.
