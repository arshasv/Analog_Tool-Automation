# rerender-use-deferred-value

**Category:** Re-render Optimization

Defer expensive reactive values during typing/input.

- Prefer `useDeferredValue` for heavy derived UI.
- Avoid tying expensive renders to every keystroke.
