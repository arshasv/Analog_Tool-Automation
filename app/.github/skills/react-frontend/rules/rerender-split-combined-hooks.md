# rerender-split-combined-hooks

**Category:** Re-render Optimization

Split hooks with independent dependencies.

- Prefer separate effects/memos per concern.
- Avoid combined hooks that rerun on unrelated changes.
