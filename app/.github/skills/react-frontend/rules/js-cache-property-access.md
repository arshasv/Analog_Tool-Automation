# js-cache-property-access

**Category:** JavaScript Performance

Cache repeated property access in tight loops.

- Prefer local variables for frequently reused properties.
- Avoid redundant deep property reads in hot paths.
