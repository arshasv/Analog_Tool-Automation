# js-hoist-regexp

**Category:** JavaScript Performance

Create regular expressions outside loops/hot functions.

- Prefer one compiled regex reused multiple times.
- Avoid re-instantiating regex objects repeatedly.
