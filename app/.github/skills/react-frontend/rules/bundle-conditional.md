# bundle-conditional

**Category:** Bundle Size Optimization

Load optional modules only when a feature is enabled.

- Prefer condition-gated imports and code paths.
- Avoid unconditional loading for optional features.
