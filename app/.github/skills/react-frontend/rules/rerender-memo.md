# rerender-memo

**Category:** Re-render Optimization

Memoize expensive derived work and heavy subtrees.

- Prefer memoized components for costly renders.
- Avoid recomputing expensive values each render.
