# js-combine-iterations

**Category:** JavaScript Performance

Combine compatible array passes.

- Prefer one pass over chained heavy `map/filter` pipelines.
- Avoid unnecessary intermediate arrays in hot paths.
