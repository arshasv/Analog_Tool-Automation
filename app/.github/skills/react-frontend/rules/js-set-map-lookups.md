# js-set-map-lookups

**Category:** JavaScript Performance

Use `Set`/`Map` for membership and keyed retrieval.

- Prefer O(1) lookups for repeated checks.
- Avoid repeated `includes`/`find` on long arrays.
