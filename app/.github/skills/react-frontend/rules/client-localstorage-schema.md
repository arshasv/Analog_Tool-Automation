# client-localstorage-schema

**Category:** Client-Side Data Fetching

Version and minimize `localStorage` payloads.

- Prefer explicit schema versioning and migration.
- Avoid large, unbounded, or opaque cached objects.
