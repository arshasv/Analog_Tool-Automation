# js-tosorted-immutable

**Category:** JavaScript Performance

Use immutable sorting patterns where state integrity matters.

- Prefer `toSorted()` for non-mutating sort behavior.
- Avoid in-place mutation of shared arrays.
