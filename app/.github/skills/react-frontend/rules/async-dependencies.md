# async-dependencies

**Category:** Eliminating Waterfalls

Model partial dependencies so only truly dependent tasks are sequenced.

- Prefer splitting independent work and composing results.
- Avoid over-serializing mixed dependency graphs.
