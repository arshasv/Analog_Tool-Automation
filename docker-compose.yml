services:
  web:
    build: .
    container_name: xeda-web
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - /usr/local/share/pdk/sky130A:/opt/pdk:ro
    environment:
      - WORK_DIR=/app/data/designs
      - SKY130_PDK=/opt/pdk
      - PYTHONPATH=/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    restart: unless-stopped
