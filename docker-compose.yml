version: '3.8'

services:
  web:
    build: .
    container_name: xeda-web
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    environment:
      - WORK_DIR=/work
    restart: unless-stopped
